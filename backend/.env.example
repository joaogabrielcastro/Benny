# Exemplo de variáveis de ambiente para o backend
# Copie este arquivo para .env e edite os valores antes de rodar

# Connection string Postgres (use 127.0.0.1 para evitar resolução ::1/IPv6 quando necessário)
# Exemplo local (Docker / Postgres local):
# DATABASE_URL=postgresql://benny_user:senha_segura@127.0.0.1:5432/benny_db

# Exemplo Neon / remoto:
# DATABASE_URL=postgresql://<user>:<password>@<host>:<port>/<database>?sslmode=require

# Porta do servidor
PORT=3000

# Outras opções (opcionais)
# NODE_ENV=development
# STORAGE_PROVIDER=local
# AWS_S3_BUCKET=
# PLUGNOTAS_API_KEY=
